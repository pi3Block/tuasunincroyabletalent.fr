# ===========================================
# tuasunincroyabletalent.fr Worker - Project-Specific Dependencies
# ===========================================
# Base image (gpu-worker-base) already includes:
# - torch, torchaudio
# - openai-whisper, whisper-timestamped
# - librosa, scipy, numpy, soundfile, pydub
# - onnxruntime
# ===========================================

# Celery & Redis
celery[redis]>=5.4.0
redis>=5.2.0

# Database
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.9

# Audio Separation (unique to this project)
demucs>=4.0.1

# Pitch Detection (unique to this project)
torchcrepe>=0.0.22

# Audio codec handling
torchcodec>=0.1.0

# YouTube download (Optimization A: worker downloads reference directly, ~-15s first run)
yt-dlp>=2024.1.0

# Evaluation Metrics
fastdtw>=0.3.4
jiwer>=3.0.0

# Ollama Client
httpx>=0.28.0

# Observability (Langfuse v3, same version constraint as augmenter.pro)
langfuse>=3.13.0

# LLM fallback via LiteLLM unified proxy
# litellm>=1.80.0 passes sdk_integration kwarg → TypeError with langfuse 3.13.0
litellm>=1.50.0,<1.80.0

# Utils
python-dotenv>=1.0.1
