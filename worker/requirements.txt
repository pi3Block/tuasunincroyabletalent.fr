# Worker dependencies
# Note: torch and torchaudio are pre-installed in the base Docker image

# Celery & Redis
celery[redis]>=5.4.0
redis>=5.2.0

# Database
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.9

# Audio Analysis
librosa>=0.10.2
scipy>=1.14.0
numpy>=1.26.0
fastdtw>=0.3.4
jiwer>=3.0.0

# Audio I/O
soundfile>=0.12.1
pydub>=0.25.1
torchcodec>=0.1.0
yt-dlp>=2024.1.0

# ML Models (PyTorch-based)
demucs>=4.0.1
audio-separator[gpu]>=0.41.0  # BS-Roformer separation (SDR 12.97, Sprint 2.2)
openai-whisper>=20240930
whisper-timestamped>=1.15.0
swift-f0  # SwiftF0 pitch detection (CPU-only, ONNX Runtime, replaces torchcrepe)
onnxruntime>=1.17.0  # SwiftF0 + Silero VAD in whisper-timestamped
deepfilternet>=0.5.6  # DeepFilterNet3 audio denoise (CPU-only, Sprint 2.1)

# Ollama Client
httpx>=0.28.0

# Utils
python-dotenv>=1.0.1
